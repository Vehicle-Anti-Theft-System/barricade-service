"""Mock RFID tag ids (hardware would emit these as strings)."""

# Cycle order for mock trigger — must match backend-service seed.py truck rfid_tag values
MOCK_RFID_TAGS = [
    "TRK-0042",
    "TRK-0078",
    "TRK-0101",
]
